# Symphony サービス仕様

ステータス: ドラフト v1 (言語非依存)

目的: プロジェクトの作業を遂行するためにコーディングエージェントをオーケストレーションするサービスを定義すること。

## 1. 問題提起

Symphonyは、イシュートラッカー（この仕様バージョンではLinear）から継続的に作業を読み取り、各イシューに対して隔離されたワークスペースを作成し、そのワークスペース内でイシューのコーディングエージェントセッションを実行する、長時間稼働の自動化サービスである。

このサービスは、運用上の4つの問題を解決する：

- 手動スクリプトの代わりに、イシューの実行を反復可能なデーモンワークフローにする。
- エージェントの実行をイシューごとのワークスペースに隔離し、エージェントのコマンドがイシューごとのワークスペースディレクトリ内でのみ実行されるようにする。
- ワークフローポリシーをリポジトリ内（`WORKFLOW.md`）に保持し、チームがエージェントのプロンプトとランタイム設定をコードと一緒にバージョン管理できるようにする。
- 複数の同時並行のエージェント実行を運用およびデバッグするのに十分なオブザーバビリティ（可観測性）を提供する。

実装においては、信頼性と安全性の姿勢を明確に文書化することが求められる。この仕様は、単一の承認、サンドボックス、またはオペレーター確認ポリシーを要求するものではない。一部の実装は、高信頼設定を使用した信頼された環境を対象とする場合があり、別の実装は、より厳格な承認やサンドボックス化を必要とする場合がある。

重要な境界:

- Symphonyは、スケジューラー/ランナーおよびトラッカーのリーダーである。
- チケットの書き込み（状態の移行、コメント、PRリンク）は通常、ワークフロー/ランタイム環境で利用可能なツールを使用してコーディングエージェントによって実行される。
- 成功した実行は、ワークフローで定義されたハンドオフ状態（例: `Human Review`（人間によるレビュー））で終了することがあり、必ずしも`Done`（完了）である必要はない。

## 2. 目標と対象外の目標 (Goals and Non-Goals)

### 2.1 目標

- 一定の間隔でイシュートラッカーをポーリングし、制限された同時実行数で作業をディスパッチする。
- ディスパッチ、再試行、および再調整のために、単一の信頼できるオーケストレータ状態を維持する。
- イシューごとに決定論的なワークスペースを作成し、実行間でそれらを保持する。
- イシューの状態が変更されて不適格になった場合、アクティブな実行を停止する。
- 指数バックオフを使用して一時的な障害から回復する。
- リポジトリ所有の `WORKFLOW.md` 契約からランタイム動作を読み込む。
- オペレーターが視認可能なオブザーバビリティ（最低でも構造化ログ）を公開する。
- 永続的なデータベースを必要とせずに再起動時の回復をサポートする。

### 2.2 対象外の目標

- リッチなWeb UIまたはマルチテナントのコントロールプレーン。
- 特定のダッシュボードまたはターミナルUIの実装を規定すること。
- 汎用ワークフローエンジンまたは分散ジョブスケジューラ。
- チケット、PR、またはコメントの編集方法に関する組み込みのビジネスロジック。（そのロジックはワークフロープロンプトとエージェントツールに存在する。）
- コーディングエージェントとホストOSが提供するものを超える強力なサンドボックス制御を義務付けること。
- すべての実装に対して単一のデフォルト承認、サンドボックス、またはオペレーター確認姿勢を義務付けること。

## 3. システム概要

### 3.1 主なコンポーネント

1. `Workflow Loader` (ワークフローローダー)
   - `WORKFLOW.md` を読み取る。
   - YAMLフロントマターとプロンプト本文を解析する。
   - `{config, prompt_template}` を返す。

2. `Config Layer` (設定レイヤー)
   - ワークフロー設定値の型付きゲッターを公開する。
   - デフォルト値と環境変数による間接参照を適用する。
   - ディスパッチ前にオーケストレータによって使用される検証を実行する。

3. `Issue Tracker Client` (イシュートラッカークライアント)
   - アクティブな状態の候補イシューを取得する。
   - 特定のイシューIDの現在の状態を取得する（再調整用）。
   - 起動時のクリーンアップ中に、終了状態のイシューを取得する。
   - トラッカーのペイロードを安定したイシューモデルに正規化する。

4. `Orchestrator` (オーケストレータ)
   - ポーリングのティックを所有する。
   - メモリ内のランタイム状態を所有する。
   - どのイシューをディスパッチ、再試行、停止、または解放するかを決定する。
   - セッションのメトリクスと再試行キューの状態を追跡する。

5. `Workspace Manager` (ワークスペースマネージャ)
   - イシューの識別子をワークスペースのパスにマッピングする。
   - イシューごとのワークスペースディレクトリが存在することを確認する。
   - ワークスペースのライフサイクルフックを実行する。
   - 終了状態のイシューのワークスペースをクリーンアップする。

6. `Agent Runner` (エージェントランナー)
   - ワークスペースを作成する。
   - イシュー + ワークフローテンプレートからプロンプトを構築する。
   - コーディングエージェントのアプリケーションサーバークライアントを起動する。
   - エージェントの更新をオーケストレータにストリーミングで戻す。

7. `Status Surface` (ステータスサーフェス - オプション)
   - 人間が読めるランタイムステータス（例：ターミナル出力、ダッシュボード、その他のオペレーター向けビュー）を提示する。

8. `Logging` (ロギング)
   - 設定された1つ以上のシンクに構造化ランタイムログを出力する。

### 3.2 抽象化レベル

Symphonyは、以下のレイヤーに分割することで移植が最も容易になる：

1. `Policy Layer` (ポリシーレイヤー - リポジトリで定義)
   - `WORKFLOW.md` のプロンプト本文。
   - チケットの処理、検証、およびハンドオフに関するチーム固有のルール。

2. `Configuration Layer` (設定レイヤー - 型付きゲッター)
   - フロントマターを型付きランタイム設定に解析する。
   - デフォルト値、環境トークン、およびパスの正規化を処理する。

3. `Coordination Layer` (調整レイヤー - オーケストレータ)
   - ポーリングループ、イシューの適格性、同時実行、再試行、再調整。

4. `Execution Layer` (実行レイヤー - ワークスペース + エージェントサブプロセス)
   - ファイルシステムのライフサイクル、ワークスペースの準備、コーディングエージェントプロトコル。

5. `Integration Layer` (統合レイヤー - Linearアダプター)
   - トラッカーデータのAPI呼び出しと正規化。

6. `Observability Layer` (可観測性レイヤー - ログ + オプションのステータスサーフェス)
   - オーケストレータとエージェントの動作に対するオペレーターの可視性。

### 3.3 外部依存関係

- イシュートラッカーAPI（この仕様バージョンでは `tracker.kind: linear` の場合はLinear）。
- ワークスペースとログのためのローカルファイルシステム。
- オプションのワークスペースへのデータ追加ツール（例：使用される場合のGit CLI）。
- stdio経由でJSON-RPC風のアプリケーションサーバーモードをサポートするコーディングエージェントの実行可能ファイル。
- イシュートラッカーおよびコーディングエージェントのためのホスト環境認証。

## 4. コアドメインモデル

### 4.1 エンティティ

#### 4.1.1 Issue (イシュー)

オーケストレーション、プロンプトのレンダリング、およびオブザーバビリティ出力に使用される正規化されたイシューレコード。

フィールド：

- `id` (文字列)
  - 安定したトラッカー内部のID。
- `identifier` (文字列)
  - 人間が読めるチケットキー（例：`ABC-123`）。
- `title` (文字列)
- `description` (文字列または null)
- `priority` (整数または null)
  - 数字が小さいほどディスパッチのソートにおける優先順位が高い。
- `state` (文字列)
  - 現在のトラッカー状態名。
- `branch_name` (文字列または null)
  - 利用可能な場合、トラッカーが提供するブランチメタデータ。
- `url` (文字列または null)
- `labels` (文字列のリスト)
  - 小文字に正規化される。
- `blocked_by` (ブロッカー参照のリスト)
  - 各ブロッカー参照には以下が含まれる：
    - `id` (文字列または null)
    - `identifier` (文字列または null)
    - `state` (文字列または null)
- `created_at` (タイムスタンプまたは null)
- `updated_at` (タイムスタンプまたは null)

#### 4.1.2 Workflow Definition (ワークフロー定義)

解析された `WORKFLOW.md` ペイロード：

- `config` (マップ)
  - YAMLフロントマターのルートオブジェクト。
- `prompt_template` (文字列)
  - フロントマター後のMarkdownの本文。トリム（空白除去）される。

#### 4.1.3 Service Config (Typed View) (サービス設定 - 型付きビュー)

`WorkflowDefinition.config` と環境変数の解決から派生した、型付きのランタイム値。

例：

- ポーリング間隔
- ワークスペースのルート
- アクティブ状態と終了状態のイシュー状態
- 同時実行制限
- コーディングエージェントの実行ファイル/引数/タイムアウト
- ワークスペースフック

#### 4.1.4 Workspace (ワークスペース)

1つのイシュー識別子に割り当てられたファイルシステム上のワークスペース。

フィールド (論理的)：

- `path` (ワークスペースのパス。現在のランタイムは通常絶対パスを使用するが、パス区切り文字なしで設定された場合は相対ルートも可能)
- `workspace_key` (サニタイズされたイシュー識別子)
- `created_now` (ブール値、`after_create` フックの実行可否の判断に使用される)

#### 4.1.5 Run Attempt (実行の試行)

1つのイシューに対する1回の実行の試行。

フィールド (論理的)：

- `issue_id`
- `issue_identifier`
- `attempt` (整数または null、最初の実行では `null`、再試行/継続では `>=1`)
- `workspace_path`
- `started_at`
- `status`
- `error` (オプション)

#### 4.1.6 Live Session (ライブセッション - エージェントセッションメタデータ)

コーディングエージェントのサブプロセスが実行されている間に追跡される状態。

フィールド：

- `session_id` (文字列、`<thread_id>-<turn_id>`)
- `thread_id` (文字列)
- `turn_id` (文字列)
- `codex_app_server_pid` (文字列または null)
- `last_codex_event` (文字列/列挙型または null)
- `last_codex_timestamp` (タイムスタンプまたは null)
- `last_codex_message` (要約されたペイロード)
- `codex_input_tokens` (整数)
- `codex_output_tokens` (整数)
- `codex_total_tokens` (整数)
- `last_reported_input_tokens` (整数)
- `last_reported_output_tokens` (整数)
- `last_reported_total_tokens` (整数)
- `turn_count` (整数)
  - 現在のワーカーのライフタイム内に開始されたコーディングエージェントのターン数。

#### 4.1.7 Retry Entry (再試行エントリ)

イシューのスケジュールされた再試行状態。

フィールド：

- `issue_id`
- `identifier` (ステータスサーフェス/ログ用のベストエフォートな人間向けID)
- `attempt` (整数、再試行キューのための1ベースの値)
- `due_at_ms` (モノトニッククロックのタイムスタンプ)
- `timer_handle` (ランタイム固有のタイマー参照)
- `error` (文字列または null)

#### 4.1.8 Orchestrator Runtime State (オーケストレータランタイム状態)

オーケストレータによって所有される、単一の信頼できるインメモリ状態。

フィールド：

- `poll_interval_ms` (現在の有効なポーリング間隔)
- `max_concurrent_agents` (現在の有効なグローバル同時実行制限)
- `running` (マップ `issue_id -> 実行中エントリ`)
- `claimed` (予約済み/実行中/再試行中のイシューIDのセット)
- `retry_attempts` (マップ `issue_id -> RetryEntry`)
- `completed` (イシューIDのセット。記録用のみで、ディスパッチの制御には使われない)
- `codex_totals` (集計されたトークン + ランタイム秒数)
- `codex_rate_limits` (エージェントイベントからの最新のレート制限スナップショット)

### 4.2 安定した識別子と正規化ルール

- `Issue ID`
  - トラッカーの検索と内部マップキーに使用する。
- `Issue Identifier`
  - 人間が読めるログとワークスペースの命名に使用する。
- `Workspace Key`
  - `issue.identifier` から派生し、`[A-Za-z0-9._-]` 以外の文字を `_` に置換する。
  - サニタイズされた値をワークスペースディレクトリ名に使用する。
- `Normalized Issue State`
  - 状態を比較する際は `lowercase` （小文字）にしてから比較する。
- `Session ID`
  - コーディングエージェントの `thread_id` と `turn_id` から `<thread_id>-<turn_id>` として構成する。

## 5. ワークフロー仕様 (リポジトリ契約)

### 5.1 ファイルの検出とパスの解決

ワークフローファイルパスの優先順位：

1. 明示的なアプリケーション/ランタイム設定（CLI起動パスによる設定）。
2. デフォルト：現在のプロセスの作業ディレクトリ内の `WORKFLOW.md`。

ローダーの動作：

- ファイルを読み取れない場合は、`missing_workflow_file` エラーを返す。
- ワークフローファイルは、リポジトリが所有し、バージョン管理されていることが想定される。

### 5.2 ファイル形式

`WORKFLOW.md` は、オプションでYAMLフロントマターを持つMarkdownファイルである。

設計上の注意：

- `WORKFLOW.md` は、アウトオブバンドのサービス固有の設定を必要とせずに、異なるワークフロー（プロンプト、ランタイム設定、フック、トラッカーの選択/設定）を記述して実行できる程度に自己完結している必要がある。

解析ルール：

- ファイルが `---` で始まる場合、次の `---` までの行をYAMLフロントマターとして解析する。
- 残りの行はプロンプトの本文になる。
- フロントマターが存在しない場合、ファイル全体をプロンプト本文として扱い、空の設定マップを使用する。
- YAMLフロントマターはマップ/オブジェクトにデコードできなければならない。マップではないYAMLはエラーである。
- プロンプト本文は、使用前にトリム（空白除去）される。

返されるワークフローオブジェクト：

- `config`: フロントマターのルートオブジェクト（`config` キーの下にネストされない）。
- `prompt_template`: トリムされたMarkdownの本文。

### 5.3 フロントマタースキーマ

トップレベルキー：

- `tracker`
- `polling`
- `workspace`
- `hooks`
- `agent`
- `codex`

前方互換性のため、不明なキーは無視されるべきである。

注意：

- ワークフローのフロントマターは拡張可能である。オプションの拡張により、上記の中核となるスキーマを変更することなく、追加のトップレベルキー（例：`server`）を定義できる。
- 拡張機能は、そのフィールドスキーマ、デフォルト値、検証ルール、および変更が動的に適用されるか再起動が必要かを文書化すべきである。
- 一般的な拡張：`server.port`（整数）は、セクション13.7で説明されているオプションのHTTPサーバーを有効にする。

#### 5.3.1 `tracker` (オブジェクト)

フィールド：

- `kind` (文字列)
  - ディスパッチに必須。
  - 現在サポートされている値：`linear`
- `endpoint` (文字列)
  - `tracker.kind == "linear"` の場合のデフォルト：`https://api.linear.app/graphql`
- `api_key` (文字列)
  - リテラルトークンまたは `$VAR_NAME`。
  - `tracker.kind == "linear"` の場合の正規の環境変数：`LINEAR_API_KEY`。
  - `$VAR_NAME` が空文字列として解決された場合、キーは存在しないものとして扱う。
- `project_slug` (文字列)
  - `tracker.kind == "linear"` の場合、ディスパッチに必須。
- `active_states` (文字列のリスト)
  - デフォルト：`Todo`, `In Progress`
- `terminal_states` (文字列のリスト)
  - デフォルト：`Closed`, `Cancelled`, `Canceled`, `Duplicate`, `Done`

#### 5.3.2 `polling` (オブジェクト)

フィールド：

- `interval_ms` (整数または文字列としての整数)
  - デフォルト：`30000`
  - 変更は実行時に再適用され、再起動なしで将来のティックスケジューリングに影響を与えるべきである。

#### 5.3.3 `workspace` (オブジェクト)

フィールド：

- `root` (パス文字列または `$VAR`)
  - デフォルト：`<system-temp>/symphony_workspaces`
  - `~` とパス区切り文字を含む文字列は展開される。
  - パス区切り文字のない単純な文字列はそのまま保持される（相対ルートは許可されるが推奨されない）。

#### 5.3.4 `hooks` (オブジェクト)

フィールド：

- `after_create` (複数行のシェルスクリプト文字列、オプション)
  - ワークスペースディレクトリが新しく作成された場合にのみ実行される。
  - 失敗するとワークスペースの作成が中止される。
- `before_run` (複数行のシェルスクリプト文字列、オプション)
  - ワークスペースの準備後、コーディングエージェントの起動前に、各エージェントの試行前に実行される。
  - 失敗すると現在の試行が中止される。
- `after_run` (複数行のシェルスクリプト文字列、オプション)
  - ワークスペースが存在すると、各エージェントの試行後（成功、失敗、タイムアウト、またはキャンセル）に実行される。
  - 失敗はログに記録されるが無視される。
- `before_remove` (複数行のシェルスクリプト文字列、オプション)
  - ディレクトリが存在する場合、ワークスペースの削除前に実行される。
  - 失敗はログに記録されるが無視される。クリーンアップはそのまま進行する。
- `timeout_ms` (整数、オプション)
  - デフォルト：`60000`
  - すべてのワークスペースフックに適用される。
  - 正でない値は無効として扱われ、デフォルトにフォールバックする。
  - 変更は実行時に再適用され、将来のフック実行に影響を与える。

#### 5.3.5 `agent` (オブジェクト)

フィールド：

- `max_concurrent_agents` (整数または文字列としての整数)
  - デフォルト：`10`
  - 変更は実行時に再適用され、その後のディスパッチの決定に影響を与えるべきである。
- `max_retry_backoff_ms` (整数または文字列としての整数)
  - デフォルト：`300000` (5分)
  - 変更は実行時に再適用され、将来の再試行スケジューリングに影響を与えるべきである。
- `max_concurrent_agents_by_state` (マップ `状態名 -> 正の整数`)
  - デフォルト：空のマップ。
  - 状態キーはルックアップのために正規化（`lowercase`）される。
  - 無効なエントリ（正でない、または数値でない）は無視される。

#### 5.3.6 `codex` (オブジェクト)

フィールド：

`approval_policy`、`thread_sandbox`、`turn_sandbox_policy` などのCodexが所有する設定値について、サポートされる値はターゲットとなるCodexアプリサーバーのバージョンによって定義される。
実装者は、この仕様で手動管理される列挙型(enum)に依存するのではなく、これらをパススルーのCodex設定値として扱うべきである。インストールされているCodexスキーマを調べるには、`codex app-server generate-json-schema --out <dir>` を実行し、`v2/ThreadStartParams.json` および `v2/TurnStartParams.json` によって参照される関連する定義を調べる。実装は、より厳密な起動チェックを行いたい場合、これらのフィールドをローカルで検証してもよい。

- `command` (文字列シェルコマンド)
  - デフォルト：`codex app-server`
  - ランタイムは、このコマンドをワークスペースディレクトリ内の `bash -lc` 経由で起動する。
  - 起動されたプロセスは、stdio経由で互換性のあるアプリケーションサーバープロトコルを話す必要がある。
- `approval_policy` (Codexの `AskForApproval` 値)
  - デフォルト：実装定義。
- `thread_sandbox` (Codexの `SandboxMode` 値)
  - デフォルト：実装定義。
- `turn_sandbox_policy` (Codexの `SandboxPolicy` 値)
  - デフォルト：実装定義。
- `turn_timeout_ms` (整数)
  - デフォルト：`3600000` (1時間)
- `read_timeout_ms` (整数)
  - デフォルト：`5000`
- `stall_timeout_ms` (整数)
  - デフォルト：`300000` (5分)
  - `<= 0` の場合、ストール検出は無効になる。

### 5.4 プロンプトテンプレート契約

`WORKFLOW.md` のMarkdown本文は、イシューごとのプロンプトテンプレートである。

レンダリングの要件：

- 厳密なテンプレートエンジンを使用する（Liquid互換のセマンティクスで十分である）。
- 不明な変数はレンダリングを失敗させる必要がある。
- 不明なフィルターはレンダリングを失敗させる必要がある。

テンプレートの入力変数：

- `issue` (オブジェクト)
  - ラベルとブロッカーを含む、すべての正規化されたイシューフィールドが含まれる。
- `attempt` (整数または null)
  - 最初の試行では `null` または不在。
  - 再試行または継続実行では整数。

フォールバックプロンプトの動作：

- ワークフロープロンプトの本文が空の場合、ランタイムは最小限のデフォルトプロンプト（`You are working on an issue from Linear.`）を使用してもよい。
- ワークフローファイルの読み取り/解析の失敗は設定/検証エラーであり、黙ってプロンプトにフォールバックするべきではない。

### 5.5 ワークフロー検証とエラーサーフェス

エラークラス：

- `missing_workflow_file`
- `workflow_parse_error`
- `workflow_front_matter_not_a_map`
- `template_parse_error` (プロンプトのレンダリング中)
- `template_render_error` (不明な変数/フィルター、無効な補間)

ディスパッチゲートの動作：

- ワークフローファイルの読み取り/YAMLエラーは、修正されるまで新しいディスパッチをブロックする。
- テンプレートエラーは、影響を受ける実行の試行のみを失敗させる。

## 6. 設定の仕様

### 6.1 ソースの優先順位と解決のセマンティクス

設定の優先順位：

1. ワークフローファイルのパスの選択（ランタイム設定 -> cwd（カレントディレクトリ）のデフォルト）。
2. YAMLフロントマターの値。
3. 選択されたYAML値内の `$VAR_NAME` による環境変数の間接参照。
4. 組み込みのデフォルト値。

値の強制（Coercion）セマンティクス：

- パス/コマンドフィールドのサポート：
  - `~` によるホームディレクトリ展開
  - 環境変数ベースのパス値のための `$VAR` 展開
  - 展開は、ローカルファイルシステムパスを意図した値にのみ適用する。URIや任意のシェルコマンド文字列を書き換えないこと。

### 6.2 動的リロードのセマンティクス

動的リロードは必須である：

- ソフトウェアは、`WORKFLOW.md` の変更を監視（watch）する必要がある。
- 変更があった場合、再起動せずにワークフロー設定とプロンプトテンプレートを再読み込みし、再適用する必要がある。
- ソフトウェアは、将来の実行に向けて、実行中の動作（例：ポーリングのペース、同時実行制限、アクティブ/終了状態、codex設定、ワークスペースのパス/フック、プロンプトのコンテンツ）を新しい設定に調整しようと試みる必要がある。
- リロードされた設定は、将来のディスパッチ、再試行のスケジュール、再調整の決定、フックの実行、およびエージェントの起動に適用される。
- 設定が変更されたときに、進行中のエージェントセッションを自動的に再起動することは実装には求められない。
- 独自のリスナー/リソースを管理する拡張機能（例：HTTPサーバーのポートの変更）は、実装がライブ再バインドを明示的にサポートしていない限り、再起動が必要になる場合がある。
- 実装は、ファイルシステム監視イベントを見逃した場合に備えて、実行時の操作中（例：ディスパッチ前）に防御的に再検証/リロードを行うべきである。
- 無効なリロードによってサービスがクラッシュしてはならない。最後に確認された有効な構成で動作を継続し、オペレーターが視認可能なエラーを出力すること。

### 6.3 ディスパッチの事前検証 (Preflight Validation)

この検証は、新しい作業をディスパッチしようとする前に実行されるスケジューラの事前検証である。考えられるすべてのワークフローの動作を完全に監査するのではなく、ワーカーのポーリングと起動に必要なワークフロー/設定を検証する。

起動時の検証：

- スケジューリングループを開始する前に構成を検証する。
- 起動時の検証が失敗した場合、起動を失敗させ、オペレーターが視認可能なエラーを出力する。

ティック（Tick）ごとのディスパッチ検証：

- 各ディスパッチサイクルの前に再検証する。
- 検証が失敗した場合、そのティックのディスパッチをスキップし、再調整をアクティブなままにし、オペレーターが視認可能なエラーを出力する。

検証のチェック項目：

- ワークフローファイルを読み込み、解析できること。
- `tracker.kind` が存在し、サポートされていること。
- `$` 解決後に `tracker.api_key` が存在すること。
- 選択されたトラッカーの種類で要求されている場合、`tracker.project_slug` が存在すること。
- `codex.command` が存在し、空でないこと。

### 6.4 設定フィールドの概要 (チートシート)

このセクションは、コーディングエージェントが設定レイヤーを素早く実装できるように意図的に冗長に記述されている。

- `tracker.kind`: 文字列、必須、現在は `linear`
- `tracker.endpoint`: 文字列、`tracker.kind=linear` の場合のデフォルトは `https://api.linear.app/graphql`
- `tracker.api_key`: 文字列または `$VAR`、`tracker.kind=linear` の場合の正規の環境変数は `LINEAR_API_KEY`
- `tracker.project_slug`: 文字列、`tracker.kind=linear` の場合は必須
- `tracker.active_states`: 文字列のリスト、デフォルトは `["Todo", "In Progress"]`
- `tracker.terminal_states`: 文字列のリスト、デフォルトは `["Closed", "Cancelled", "Canceled", "Duplicate", "Done"]`
- `polling.interval_ms`: 整数、デフォルト `30000`
- `workspace.root`: パス、デフォルト `<system-temp>/symphony_workspaces`
- `worker.ssh_hosts` (拡張): SSHホスト文字列のリスト、オプション。省略された場合、作業はローカルで実行される。
- `worker.max_concurrent_agents_per_host` (拡張): 正の整数、オプション。設定されたSSHホスト全体に適用される、ホストごとの共有上限。
- `hooks.after_create`: シェルスクリプトまたは null
- `hooks.before_run`: シェルスクリプトまたは null
- `hooks.after_run`: シェルスクリプトまたは null
- `hooks.before_remove`: シェルスクリプトまたは null
- `hooks.timeout_ms`: 整数、デフォルト `60000`
- `agent.max_concurrent_agents`: 整数、デフォルト `10`
- `agent.max_turns`: 整数、デフォルト `20`
- `agent.max_retry_backoff_ms`: 整数、デフォルト `300000` (5分)
- `agent.max_concurrent_agents_by_state`: 正の整数のマップ、デフォルト `{}`
- `codex.command`: シェルコマンド文字列、デフォルト `codex app-server`
- `codex.approval_policy`: Codex `AskForApproval` 値、デフォルトは実装定義
- `codex.thread_sandbox`: Codex `SandboxMode` 値、デフォルトは実装定義
- `codex.turn_sandbox_policy`: Codex `SandboxPolicy` 値、デフォルトは実装定義
- `codex.turn_timeout_ms`: 整数、デフォルト `3600000`
- `codex.read_timeout_ms`: 整数、デフォルト `5000`
- `codex.stall_timeout_ms`: 整数、デフォルト `300000`
- `server.port` (拡張): 整数、オプション。オプションのHTTPサーバーを有効にする。一時的なローカルバインドの要求には `0` を使用でき、CLIの `--port` がこれを上書きする。

## 7. オーケストレーションの状態遷移 (State Machine)

オーケストレータは、スケジューリング状態を変化させる唯一のコンポーネントである。ワーカーのすべての結果はオーケストレータに報告され、明示的な状態遷移に変換される。

### 7.1 イシューのオーケストレーション状態

これはトラッカーの状態（`Todo`、`In Progress` など）と同じではない。これはサービスの内部的な要求(claim)状態である。

1. `Unclaimed` (未要求)
   - イシューは実行されておらず、再試行もスケジュールされていない。

2. `Claimed` (要求済み)
   - オーケストレータは、重複するディスパッチを防ぐためにイシューを予約している。
   - 実際には、要求済みのイシューは `Running` または `RetryQueued` のいずれかになる。

3. `Running` (実行中)
   - ワーカータスクが存在し、イシューは `running` マップ内で追跡されている。

4. `RetryQueued` (再試行キュー入り)
   - ワーカーは実行されていないが、再試行タイマーが `retry_attempts` に存在する。

5. `Released` (解放済み)
   - イシューが終了状態、非アクティブ、見つからない、または再ディスパッチなしで再試行パスが完了したため、要求が削除された。

重要なニュアンス：

- ワーカーが正常に終了したことは、イシューが完全に完了したことを意味するわけではない。
- ワーカーは、終了する前に複数のバックツーバックのコーディングエージェントターンを継続する場合がある。
- 通常のターンが完了するたびに、ワーカーはトラッカーのイシュー状態を再確認する。
- イシューがまだアクティブな状態である場合、ワーカーは、最大 `agent.max_turns` まで、同じワークスペース内の同じライブコーディングエージェントスレッドで別のターンを開始する必要がある。
- 最初のターンでは、レンダリングされたタスクプロンプト全体を使用する必要がある。
- 継続するターンでは、既存のスレッド履歴に既に存在する元のタスクプロンプトを再送信するのではなく、既存のスレッドに継続のガイダンスのみを送信する必要がある。
- ワーカーが正常に終了した後も、オーケストレータは短い継続の再試行（約1秒）をスケジュールし、イシューが引き続きアクティブであり、別のワーカーセッションが必要かどうかを再確認できるようにする。

### 7.2 Run Attempt (実行の試行) のライフサイクル

実行の試行は、以下のフェーズを移行する：

1. `PreparingWorkspace` (ワークスペースの準備中)
2. `BuildingPrompt` (プロンプトの構築中)
3. `LaunchingAgentProcess` (エージェントプロセスの起動中)
4. `InitializingSession` (セッションの初期化中)
5. `StreamingTurn` (ターンのストリーミング中)
6. `Finishing` (終了処理中)
7. `Succeeded` (成功)
8. `Failed` (失敗)
9. `TimedOut` (タイムアウト)
10. `Stalled` (ストール(停滞))
11. `CanceledByReconciliation` (再調整によるキャンセル)

再試行ロジックとログが異なるため、終了の理由を区別することが重要である。

### 7.3 遷移のトリガー

- `Poll Tick` (ポーリングのティック)
  - 実行中のイシューを再調整する。
  - 設定を検証する。
  - 候補のイシューを取得する。
  - スロットがなくなるまでディスパッチする。

- `Worker Exit (normal)` (ワーカーの正常終了)
  - 実行中エントリを削除する。
  - 合計ランタイムを更新する。
  - ワーカーがプロセス内のターンループを使い果たすか終了した後、継続の再試行 (attempt `1`) をスケジュールする。

- `Worker Exit (abnormal)` (ワーカーの異常終了)
  - 実行中エントリを削除する。
  - 合計ランタイムを更新する。
  - 指数バックオフの再試行をスケジュールする。

- `Codex Update Event` (Codex更新イベント)
  - ライブセッションフィールド、トークンカウンター、およびレート制限を更新する。

- `Retry Timer Fired` (再試行タイマー発火)
  - アクティブな候補を再取得して再ディスパッチを試みるか、不適格になった場合は要求を解放する。

- `Reconciliation State Refresh` (再調整のための状態の更新)
  - イシューの状態が終了または非アクティブになった実行を停止する。

- `Stall Timeout` (ストールタイムアウト)
  - ワーカーを強制終了し、再試行をキューに入れる。

### 7.4 冪等性と回復のルール

- オーケストレータは、重複するディスパッチを避けるために、1つの権限を通じて状態の変更をシリアル化する。
- ワーカーを起動する前に、`claimed` および `running` のチェックが必要である。
- 再調整(Reconciliation)は、すべてのティックのディスパッチ前に実行される。
- 再起動時の回復は、トラッカー主導およびファイルシステム主導である（永続的なオーケストレータDBは必要ない）。
- 起動時の終了クリーンアップにより、既に終了状態にあるイシューの古くなったワークスペースが削除される。

## 8. ポーリング、スケジューリング、および再調整

### 8.1 ポーリングループ

起動時に、サービスは設定を検証し、起動時のクリーンアップを実行し、即時のティック（tick）をスケジュールし、その後 `polling.interval_ms` ごとに繰り返す。

ワークフロー設定の変更が再適用される際には、有効なポーリング間隔を更新する必要がある。

ティックのシーケンス：

1. 実行中のイシューを再調整(Reconcile)する。
2. ディスパッチの事前検証を実行する。
3. トラッカーからアクティブな状態の候補イシューを取得する。
4. イシューをディスパッチ優先度でソートする。
5. スロットが残っている間、適格なイシューをディスパッチする。
6. オブザーバビリティ/ステータスのコンシューマーに状態の変更を通知する。

ティックごとの検証が失敗した場合、そのティックのディスパッチはスキップされるが、再調整は最初に実行される。

### 8.2 候補の選択ルール

イシューは、以下のすべてが真である場合にのみディスパッチ適格となる：

- `id`、`identifier`、`title`、および `state` を持っていること。
- その状態が `active_states` に含まれており、`terminal_states` に含まれていないこと。
- まだ `running` に存在しないこと。
- まだ `claimed` に存在しないこと。
- グローバルな同時実行スロットが利用可能であること。
- 状態ごとの同時実行スロットが利用可能であること。
- `Todo` 状態に関するブロッカーのルールに合格していること：
  - イシューの状態が `Todo` の場合、いずれかのブロッカーが終了状態（non-terminal）でない場合はディスパッチしない。

ソート順（安定した意図）：

1. `priority` の昇順（1〜4が優先される。null/不明は最後にソートされる）
2. `created_at` の古いもの順
3. `identifier` の辞書順（タイブレーク）

### 8.3 同時実行制御

グローバル制限：

- `available_slots = max(max_concurrent_agents - running_count, 0)`

状態ごとの制限：

- 存在する場合は `max_concurrent_agents_by_state[state]`（状態キーは正規化される）
- それ以外の場合はグローバル制限にフォールバックする

ランタイムは、`running` マップ内で追跡されている現在の状態によってイシューをカウントする。

オプションのSSHホスト制限：

- `worker.max_concurrent_agents_per_host` が設定されている場合、設定された各SSHホストは一度にその数以下の同時エージェントを実行できる。
- その上限に達しているホストは、容量が空くまで新しいディスパッチの対象からスキップされる。

### 8.4 再試行とバックオフ

再試行エントリの作成：

- 同じイシューに対する既存の再試行タイマーをすべてキャンセルする。
- `attempt`、`identifier`、`error`、`due_at_ms`、および新しいタイマーのハンドルを保存する。

バックオフの計算式：

- クリーンなワーカー終了後の通常の継続再試行は、`1000` ms の短い固定遅延を使用する。
- 障害による再試行は、`delay = min(10000 * 2^(attempt - 1), agent.max_retry_backoff_ms)` を使用する。
- 乗数は、設定された最大再試行バックオフ（デフォルト `300000` / 5分）によって上限が定められる。

再試行の処理動作：

1. アクティブな候補イシューを取得する（すべてのイシューではない）。
2. `issue_id` によって特定のイシューを探す。
3. 見つからない場合は、要求（claim）を解放する。
4. 見つかり、かつ候補として適格である場合：
   - スロットが利用可能であればディスパッチする。
   - それ以外の場合は、`no available orchestrator slots`（利用可能なオーケストレータスロットがありません）というエラーで再キューイングする。
5. 見つかったがアクティブではなくなっている場合は、要求を解放する。

注意：

- 終了状態のワークスペースのクリーンアップは、起動時のクリーンアップおよびアクティブ実行の再調整（現在実行中のイシューの終了状態への移行を含む）によって処理される。
- 再試行処理は、主にアクティブな候補を操作し、イシューが存在しない場合は要求を解放するのみで、クリーンアップ自体は実行しない。

### 8.5 アクティブな実行の再調整 (Active Run Reconciliation)

再調整は各ティックごとに実行され、2つの部分から成る。

パートA: ストール検出

- 実行中の各イシューについて、以下からの `elapsed_ms` (経過ミリ秒) を計算する：
  - イベントが観察された場合は `last_codex_timestamp`、それ以外は
  - `started_at`
- `elapsed_ms > codex.stall_timeout_ms` の場合、ワーカーを終了させ、再試行をキューに入れる。
- `stall_timeout_ms <= 0` の場合、ストール検出を完全にスキップする。

パートB: トラッカー状態の更新

- 実行中のすべてのイシューIDの現在のイシュー状態を取得する。
- 実行中の各イシューについて：
  - トラッカーの状態が終了(terminal)の場合：ワーカーを終了し、ワークスペースをクリーンアップする。
  - トラッカーの状態がまだアクティブの場合：インメモリのイシュースナップショットを更新する。
  - トラッカーの状態がアクティブでも終了でもない場合：ワークスペースをクリーンアップせずにワーカーを終了する。
- 状態の更新に失敗した場合は、ワーカーを実行したままにし、次のティックで再試行する。

### 8.6 起動時の終了ワークスペースのクリーンアップ

サービス起動時：

1. 終了状態にあるイシューをトラッカーに照会する。
2. 返されたイシュー識別子ごとに、対応するワークスペースディレクトリを削除する。
3. 終了イシューの取得に失敗した場合、警告をログに記録し、起動を続行する。

これにより、再起動後に古い終了ワークスペースが蓄積されるのを防ぐ。

## 9. ワークスペースの管理と安全性

### 9.1 ワークスペースのレイアウト

ワークスペースのルート：

- `workspace.root`（正規化されたパス。現在の設定レイヤーでは、パスに似た値を展開し、単なる相対パス名は保持する）

イシューごとのワークスペースのパス：

- `<workspace.root>/<sanitized_issue_identifier>`

ワークスペースの永続性：

- ワークスペースは、同じイシューに対する実行間で再利用される。
- 成功した実行によってワークスペースが自動的に削除されることはない。

### 9.2 ワークスペースの作成と再利用

入力: `issue.identifier`

アルゴリズムの概要：

1. 識別子を `workspace_key` にサニタイズする。
2. ワークスペースルートの下のワークスペースパスを計算する。
3. ワークスペースパスがディレクトリとして存在することを確認する。
4. この呼び出し中にディレクトリが作成された場合にのみ `created_now=true` とマークし、それ以外の場合は `created_now=false` とする。
5. `created_now=true` の場合、設定されていれば `after_create` フックを実行する。

注意：

- このセクションでは、特定のリポジトリ/VCSワークフローを想定していない。
- ディレクトリ作成以外のワークスペースの準備（例：依存関係のブートストラップ、チェックアウト/同期、コード生成）は実装定義であり、通常はフックを介して処理される。

### 9.3 オプションのワークスペースのデータ追加（実装定義）

仕様では、組み込みのVCSやリポジトリのブートストラップ動作は必須としていない。

実装は、実装定義のロジックおよび/またはフック（例：`after_create` または `before_run`）を使用して、ワークスペースのデータの追加または同期を行うことができる。

失敗の処理：

- ワークスペースのデータ追加/同期の失敗は、現在の試行のエラーを返す。
- 新しいワークスペースの作成中に失敗した場合、実装は部分的に準備されたディレクトリを削除してもよい。
- 再利用されたワークスペースは、そのポリシーが明示的に選択および文書化されていない限り、データの追加の失敗時に破壊的にリセットされるべきではない。

### 9.4 ワークスペースフック

サポートされているフック：

- `hooks.after_create`
- `hooks.before_run`
- `hooks.after_run`
- `hooks.before_remove`

実行契約：

- `cwd` をワークスペースディレクトリとして、ホストOSに適したローカルシェルコンテキストで実行する。
- POSIXシステムでは、`sh -lc <script>`（または `bash -lc <script>` のようなより厳密な同等物）が適合するデフォルトである。
- フックのタイムアウトには `hooks.timeout_ms` が使用される。デフォルトは `60000 ms`。
- フックの開始、失敗、タイムアウトをログに記録する。

失敗のセマンティクス：

- `after_create` の失敗またはタイムアウトは、ワークスペースの作成において致命的である。
- `before_run` の失敗またはタイムアウトは、現在の実行試行において致命的である。
- `after_run` の失敗またはタイムアウトはログに記録され、無視される。
- `before_remove` の失敗またはタイムアウトはログに記録され、無視される。

### 9.5 安全性の不変条件

これは最も重要な移植性の制約である。

不変条件1: コーディングエージェントは、イシューごとのワークスペースパスでのみ実行する。

- コーディングエージェントのサブプロセスを起動する前に、以下を検証する：
  - `cwd == workspace_path`

不変条件2: ワークスペースのパスは、ワークスペースのルート内に留まらなければならない。

- 両方のパスを絶対パスに正規化する。
- `workspace_path` がプレフィックスディレクトリとして `workspace_root` を持っていることを要求する。
- ワークスペースルート外のパスはすべて拒否する。

不変条件3: ワークスペースキーはサニタイズされる。

- ワークスペースのディレクトリ名には `[A-Za-z0-9._-]` のみが許可される。
- それ以外の文字はすべて `_` に置換する。

## 10. エージェントランナープロトコル (コーディングエージェントの統合)

このセクションでは、コーディングエージェントのアプリケーションサーバーを統合するための、言語に依存しない契約を定義する。

互換性プロファイル：

- 規範的な契約は、メッセージの順序、必須の動作、および抽出する必要がある論理フィールド（例：セッションID、完了状態、承認処理、使用状況/レート制限のテレメトリ）である。
- 正確なJSONフィールド名は、互換性のあるアプリケーションサーバーのバージョンによって若干異なる場合がある。
- 実装は、特にネストされたID、承認要求、ユーザー入力要求のシグナル、およびトークン/レート制限メタデータに関して、同じ論理的意味を持つ場合、等価なペイロード形状を許容すべきである。

### 10.1 起動の契約

サブプロセスの起動パラメータ：

- コマンド：`codex.command`
- 呼び出し：`bash -lc <codex.command>`
- 作業ディレクトリ：ワークスペースのパス
- Stdout(標準出力)/stderr(標準エラー出力)：別々のストリーム
- フレーミング：stdout上の行区切りのプロトコルメッセージ（1行につき1つのJSON-RPC風のJSON）

注意：

- デフォルトのコマンドは `codex app-server` である。
- 承認ポリシー、cwd、およびプロンプトは、セクション10.2のプロトコルメッセージで表現される。

推奨される追加のプロセス設定：

- 最大行サイズ：10 MB（安全なバッファリングのため）

### 10.2 セッション開始のハンドシェイク

参考：https://developers.openai.com/codex/app-server/

クライアントは以下のプロトコルメッセージを順番に送信しなければならない：

説明的な起動トランスクリプト（同じセマンティクスを維持していれば、同等のペイロード形状も許容される）：

```json
{"id":1,"method":"initialize","params":{"clientInfo":{"name":"symphony","version":"1.0"},"capabilities":{}}}
{"method":"initialized","params":{}}
{"id":2,"method":"thread/start","params":{"approvalPolicy":"<implementation-defined>","sandbox":"<implementation-defined>","cwd":"/abs/workspace"}}
{"id":3,"method":"turn/start","params":{"threadId":"<thread-id>","input":[{"type":"text","text":"<rendered prompt-or-continuation-guidance>"}],"cwd":"/abs/workspace","title":"ABC-123: Example","approvalPolicy":"<implementation-defined>","sandboxPolicy":{"type":"<implementation-defined>"}}}
```

1. `initialize` リクエスト
   - パラメータには以下が含まれる：
     - `clientInfo` オブジェクト（例：`{name, version}`）
     - `capabilities` オブジェクト（空でもよい）
   - 対象のCodexアプリサーバーが動的ツールのための機能(capability)のネゴシエーションを必要とする場合は、ここにその機能フラグを含めること。
   - 応答を待つ（`read_timeout_ms`）
2. `initialized` 通知
3. `thread/start` リクエスト
   - パラメータには以下が含まれる：
     - `approvalPolicy` = 実装定義のセッション承認ポリシー値
     - `sandbox` = 実装定義のセッションサンドボックス値
     - `cwd` = 絶対ワークスペースパス
     - オプションのクライアントサイドツールが実装されている場合は、ターゲットのCodexアプリサーバーバージョンがサポートするプロトコルメカニズムを使用して、公開されているツール仕様を含めること。
4. `turn/start` リクエスト
   - パラメータには以下が含まれる：
     - `threadId`
     - `input` = 最初のターンの場合はレンダリングされたプロンプト、同じスレッドの後続のターンの場合は継続ガイダンスを含む単一のテキストアイテム
     - `cwd`
     - `title` = `<issue.identifier>: <issue.title>`
     - `approvalPolicy` = 実装定義のターン承認ポリシー値
     - `sandboxPolicy` = ターゲットのアプリサーバーのバージョンで要求される場合、実装定義のオブジェクト形式のサンドボックスポリシーペイロード

セッション識別子：

- `thread/start` の結果 `result.thread.id` から `thread_id` を読み取る
- 各 `turn/start` の結果 `result.turn.id` から `turn_id` を読み取る
- `session_id = "<thread_id>-<turn_id>"` を発行する
- 1つのワーカー実行内のすべての継続ターンに対して、同じ `thread_id` を再利用する

### 10.3 ストリーミングターンの処理

クライアントはターンが終了するまで行区切りのメッセージを読み取る。

完了条件：

- `turn/completed` -> 成功
- `turn/failed` -> 失敗
- `turn/cancelled` -> 失敗
- ターンタイムアウト（`turn_timeout_ms`） -> 失敗
- サブプロセスの終了 -> 失敗

継続の処理：

- 正常なターンの後、ワーカーが継続を決定した場合、同じライブの `threadId` で別の `turn/start` を発行すべきである。
- アプリサーバーのサブプロセスは、これらの継続ターンにわたってアクティブなままにし、ワーカーの実行が終了するときにのみ停止されるべきである。

行処理の要件：

- プロトコルメッセージはstdout(標準出力)からのみ読み取ること。
- 改行が到着するまで部分的なstdout行をバッファリングする。
- 完全なstdout行でJSON解析を試みる。
- Stderr(標準エラー出力)はプロトコルストリームの一部ではない：
  - 無視するか、診断としてログに記録する。
  - stderrでプロトコルJSONの解析を試みないこと。

### 10.4 発行されるランタイムイベント（オーケストレータへのアップストリーム）

アプリサーバーのクライアントは、オーケストレータのコールバックに構造化イベントを発行する。各イベントには以下を含めるべきである：

- `event` (列挙型/文字列)
- `timestamp` (UTCタイムスタンプ)
- `codex_app_server_pid` (利用可能な場合)
- オプションの `usage` マップ (トークン数)
- 必要に応じたペイロードフィールド

発行される重要なイベントの例：

- `session_started`
- `startup_failed`
- `turn_completed`
- `turn_failed`
- `turn_cancelled`
- `turn_ended_with_error`
- `turn_input_required`
- `approval_auto_approved`
- `unsupported_tool_call`
- `notification`
- `other_message`
- `malformed`

### 10.5 承認、ツール呼び出し、およびユーザー入力ポリシー

承認、サンドボックス、およびユーザー入力の動作は実装定義である。

ポリシー要件：

- 各実装は、選択した承認、サンドボックス、およびオペレーター確認の姿勢を文書化する必要がある。
- 承認要求やユーザー入力要求のイベントにより、実行が無期限に停止(ストール)したままになってはならない。実装は、文書化されたポリシーに従って、それらを満たす、オペレーターに提示する、自動的に解決する、または実行を失敗させるべきである。

高信頼(high-trust)の動作の例：

- セッションのコマンド実行承認を自動承認する。
- セッションのファイル変更承認を自動承認する。
- ユーザー入力が必要なターンをハードエラー(hard failure)として扱う。

サポートされていない動的ツール呼び出し：

- 実装およびランタイムによって明示的に公開されているサポート対象の動的ツール呼び出しは、拡張の契約に従って処理されるべきである。
- エージェントがサポートされていない動的ツールの呼び出し（`item/tool/call`）を要求した場合、ツールの失敗応答を返し、セッションを続行すること。
- これにより、サポートされていないツール実行パスでセッションが停止(ストール)するのを防ぐ。

オプションのクライアントサイドツールの拡張：

- 実装は、制限された一連のクライアントサイドツールをアプリサーバーセッションに公開してもよい。
- 現在のオプションの標準化されたツール：`linear_graphql`。
- 実装されている場合、対象のCodexアプリサーバーバージョンでサポートされているプロトコルメカニズムを使用して、起動中にサポートされているツールをアプリサーバーセッションに通知する必要がある。
- サポートされていないツール名は、引き続き失敗の結果を返し、セッションを続行すべきである。

`linear_graphql` 拡張の契約：

- 目的：現在のセッションでSymphonyに設定されたトラッカー認証を使用して、Linearに対する生のGraphQLクエリまたはミューテーションを実行する。
- 可用性：`tracker.kind == "linear"` であり、有効なLinear認証が設定されている場合にのみ意味を持つ。
- 推奨される入力形式：

  ```json
  {
    "query": "単一のGraphQLクエリまたはミューテーションドキュメント",
    "variables": {
      "optional": "graphql variables object"
    }
  }
  ```

- `query` は空ではない文字列でなければならない。
- `query` は正確に1つのGraphQLオペレーションを含む必要がある。
- `variables` はオプションであり、存在する場合はJSONオブジェクトでなければならない。
- 実装は、さらに生のGraphQLクエリ文字列を省略形の入力として受け入れてもよい。
- ツール呼び出しごとに1つのGraphQL操作を実行する。
- 提供されたドキュメントに複数の操作が含まれている場合、ツール呼び出しを無効な入力として拒否する。
- `operationName` の選択は、意図的にこの拡張の範囲外とする。
- コーディングエージェントにディスクから生のトークンを読み取らせるのではなく、アクティブなSymphonyのワークフロー/ランタイム設定で設定されたLinearのエンドポイントと認証を再利用する。
- ツール結果のセマンティクス：
  - トランスポートの成功 + 最上位のGraphQL `errors` がない -> `success=true`
  - 最上位のGraphQL `errors` が存在する -> `success=false` になるが、デバッグのためにGraphQLの応答本文を保持する
  - 無効な入力、認証の欠落、またはトランスポートの失敗 -> エラーペイロード付きで `success=false`
- モデルがセッション内で検査できる構造化されたツールの出力として、GraphQLの応答またはエラーペイロードを返す。

応答の例（同じ結果を維持していれば、同等のペイロード形状も許容される）：

```json
{"id":"<approval-id>","result":{"approved":true}}
{"id":"<tool-call-id>","result":{"success":false,"error":"unsupported_tool_call"}}
```

ユーザー入力要件でのハードエラー(Hard failure)：

- エージェントがユーザーの入力を要求した場合は、直ちに実行試行を失敗させる。
- クライアントは、以下の方法でこれを検出する：
  - 明示的なメソッド (`item/tool/requestUserInput`)、または
  - 入力が必要であることを示すターンのメソッド/フラグ。

### 10.6 タイムアウトとエラーマッピング

タイムアウト：

- `codex.read_timeout_ms`: 起動および同期リクエスト時のリクエスト/レスポンスタイムアウト
- `codex.turn_timeout_ms`: ターンストリームの合計タイムアウト
- `codex.stall_timeout_ms`: イベントの非アクティブ状態に基づいてオーケストレータによって強制される

エラーマッピング（推奨される正規化カテゴリ）：

- `codex_not_found`
- `invalid_workspace_cwd`
- `response_timeout`
- `turn_timeout`
- `port_exit`
- `response_error`
- `turn_failed`
- `turn_cancelled`
- `turn_input_required`

### 10.7 エージェントランナー契約

`Agent Runner` (エージェントランナー) は、ワークスペース + プロンプト + アプリサーバーのクライアントをラップする。

動作：

1. イシュー用のワークスペースを作成/再利用する。
2. ワークフローテンプレートからプロンプトを構築する。
3. アプリサーバーセッションを開始する。
4. アプリサーバーのイベントをオーケストレータに転送する。
5. エラーが発生した場合は、ワーカーの試行を失敗させる（オーケストレータが再試行を行う）。

注意：

- ワークスペースは、成功した実行後も意図的に保持される。
## 11. イシュートラッカー統合契約 (Linear互換)

### 11.1 必須の操作

実装は、以下のトラッカーアダプターの操作をサポートしなければならない：

1. `fetch_candidate_issues()`
   - 設定されたプロジェクトの、設定されたアクティブな状態のイシューを返す。

2. `fetch_issues_by_states(state_names)`
   - 起動時の終了状態のクリーンアップに使用される。

3. `fetch_issue_states_by_ids(issue_ids)`
   - アクティブな実行の再調整に使用される。

### 11.2 クエリのセマンティクス (Linear)

`tracker.kind == "linear"` の場合のLinear固有の要件：

- `tracker.kind == "linear"`
- GraphQLエンドポイント（デフォルト `https://api.linear.app/graphql`）
- 認証トークンは `Authorization` ヘッダーで送信される
- `tracker.project_slug` はLinearプロジェクトの `slugId` にマッピングされる
- 候補イシューのクエリは、`project: { slugId: { eq: $projectSlug } }` を使用してプロジェクトをフィルタリングする
- イシュー状態の更新クエリは、変数タイプ `[ID!]` のGraphQLイシューIDを使用する
- 候補イシューにはページネーション（ページ送り）が必要である
- ページサイズのデフォルト：`50`
- ネットワークタイムアウト：`30000 ms`

重要：

- LinearのGraphQLスキーマの詳細は変更される可能性がある。クエリの構築を隔離された状態に保ち、この仕様で要求される正確なクエリフィールド/タイプをテストすること。

Linear以外の実装ではトランスポートの詳細が変更される可能性があるが、正規化された出力はセクション4のドメインモデルと一致しなければならない。

### 11.3 正規化ルール

候補イシューの正規化では、セクション4.1.1にリストされているフィールドを生成する必要がある。

追加の正規化の詳細：

- `labels` -> 小文字の文字列
- `blocked_by` -> リレーションタイプが `blocks` である逆のリレーションから派生する
- `priority` -> 整数のみ（整数以外はnullになる）
- `created_at` と `updated_at` -> ISO-8601タイムスタンプを解析する

### 11.4 エラー処理契約

推奨されるエラーのカテゴリ：

- `unsupported_tracker_kind`
- `missing_tracker_api_key`
- `missing_tracker_project_slug`
- `linear_api_request` (トランスポートの失敗)
- `linear_api_status` (非200のHTTP)
- `linear_graphql_errors`
- `linear_unknown_payload`
- `linear_missing_end_cursor` (ページネーションの整合性エラー)

トラッカーエラー時のオーケストレータの動作：

- 候補の取得失敗：ログに記録し、このティックのディスパッチをスキップする。
- 実行中状態の更新失敗：ログに記録し、アクティブなワーカーを実行したままにする。
- 起動時の終了状態のクリーンアップ失敗：警告をログに記録し、起動を続行する。

### 11.5 トラッカーの書き込み (重要な境界)

Symphonyは、オーケストレータ内でファーストクラスのトラッカー書き込みAPIを必要としない。

- チケットの変更（状態の遷移、コメント、PRメタデータ）は、通常、ワークフロープロンプトで定義されたツールを使用してコーディングエージェントによって処理される。
- サービスは引き続きスケジューラ/ランナーおよびトラッカーのリーダーである。
- ワークフロー固有の成功は、多くの場合、トラッカーの終了状態 `Done` ではなく、「次のハンドオフ状態（例：`Human Review`）に到達した」ことを意味する。
- オプションの `linear_graphql` クライアントサイドツールの拡張機能が実装されている場合でも、それはオーケストレータのビジネスロジックではなく、エージェントツールチェーンの一部である。

## 12. プロンプトの構築とコンテキストの組み立て

### 12.1 入力

プロンプトレンダリングへの入力：

- `workflow.prompt_template`
- 正規化された `issue` オブジェクト
- オプションの `attempt` 整数（再試行/継続メタデータ）

### 12.2 レンダリングルール

- 厳密な変数チェックでレンダリングする。
- 厳密なフィルターチェックでレンダリングする。
- テンプレートの互換性のために、イシューオブジェクトのキーを文字列に変換する。
- テンプレートが反復処理できるように、ネストされた配列/マップ（ラベル、ブロッカー）を保持する。

### 12.3 再試行/継続のセマンティクス

ワークフロープロンプトは以下に対して異なる指示を提供する場合があるため、`attempt` をテンプレートに渡す必要がある。

- 初回実行（`attempt` はnullまたは存在しない）
- 以前のセッションが成功した後の継続実行
- エラー/タイムアウト/ストール後の再試行

### 12.4 失敗のセマンティクス

プロンプトのレンダリングが失敗した場合：

- ただちに実行試行を失敗させる。
- オーケストレータに他のワーカーの失敗と同様に扱わせ、再試行動作を決定させる。

## 13. ロギング、ステータス、および可観測性 (Observability)

### 13.1 ロギングの規則

イシュー関連のログに必要なコンテキストフィールド：

- `issue_id`
- `issue_identifier`

コーディングエージェントセッションのライフサイクルログに必要なコンテキスト：

- `session_id`

メッセージのフォーマット要件：

- 安定した `key=value` 形式の表現を使用する。
- アクションの結果（`completed`, `failed`, `retrying` など）を含める。
- 失敗した場合は、その理由を簡潔に含める。
- 必要がない限り、大きな生のペイロードをログに記録することは避ける。

### 13.2 ログの出力とシンク

この仕様では、ログの送信先（stderr、ファイル、リモートシンクなど）は規定していない。

要件：

- オペレーターは、デバッガーをアタッチすることなく、起動/検証/ディスパッチの失敗を確認できなければならない。
- 実装は1つ以上のシンクに書き込んでもよい。
- 設定されたログシンクが失敗した場合、サービスは可能な限り実行を継続し、残りのシンクを通じてオペレーターが視認可能な警告を発行する必要がある。

### 13.3 ランタイムスナップショット / モニタリングインターフェース (オプションだが推奨)

実装が（ダッシュボードや監視のために）同期ランタイムスナップショットを公開する場合、以下を返す必要がある：

- `running` (実行中のセッション行のリスト)
- 各実行中の行には `turn_count` を含める必要がある
- `retrying` (再試行キュー行のリスト)
- `codex_totals`
  - `input_tokens`
  - `output_tokens`
  - `total_tokens`
  - `seconds_running` (スナップショット時点でのアクティブセッションを含む合計の実行秒数)
- `rate_limits` (可能であれば、最新のコーディングエージェントのレート制限ペイロード)

推奨されるスナップショットのエラーモード：

- `timeout`
- `unavailable`

### 13.4 オプションの人間が読めるステータスサーフェス

人間が読めるステータスサーフェス（ターミナル出力、ダッシュボードなど）はオプションであり、実装定義である。

存在する場合、それはオーケストレータの状態/メトリクスからのみ描画されるべきであり、正確さのために必須であってはならない。

### 13.5 セッションメトリクスとトークンアカウンティング

トークン集計のルール：

- エージェントのイベントには、複数のペイロード形状でトークン数が含まれる場合がある。
- 以下のような、利用可能な場合はスレッドの絶対合計を優先する：
  - `thread/tokenUsage/updated` ペイロード
  - トークン数ラッパーイベント内の `total_token_usage`
  - ダッシュボード/APIの合計では、`last_token_usage` のようなデルタ（差分）スタイルのペイロードは無視する。
  - 選択したペイロード内の一般的なフィールド名から、入力/出力/合計トークン数を寛大に（leniently）抽出する。
  - 絶対的な合計については、二重カウントを避けるために最後に報告された合計との相対的なデルタを追跡する。
  - イベントタイプがそのように定義していない限り、汎用的な `usage` マップを累積合計として扱わないこと。
  - オーケストレータの状態で累積合計を集計する。

ランタイム集計：

- ランタイムは、スナップショット/レンダリング時にライブの合計として報告する必要がある。
- 実装は、終了したセッションの累積カウンターを維持し、スナップショット/ステータスビューを生成するときに `running` エントリ（例：`started_at`）から派生したアクティブセッションの経過時間を追加してもよい。
- セッションが終了したとき（通常の終了またはキャンセル/終了）、その実行の継続秒数を終了済みセッションの累積ランタイムに追加する。
- 実行時間の合計を継続的にバックグラウンドでカウントすることは必要ない。

レート制限の追跡：

- 任意のエージェントの更新で確認された最新のレート制限ペイロードを追跡する。
- レート制限データの人間が読める形式の表示は、実装で定義される。

### 13.6 人間化されたエージェントイベントの要約 (オプション)

生のエージェントプロトコルイベントの人間化された（人間が読める）要約はオプションである。

実装されている場合：

- それらはオブザーバビリティのみの出力として扱うこと。
- オーケストレータのロジックが人間化された文字列に依存しないようにすること。

### 13.7 オプションのHTTPサーバー拡張

このセクションでは、オブザーバビリティと運用制御のためのオプションのHTTPインターフェースを定義する。

実装されている場合：

- HTTPサーバーは拡張機能であり、適合性のために必須ではない。
- 実装は、ダッシュボード用にサーバー側でレンダリングされたHTMLまたはクライアント側アプリケーションを提供してもよい。
- ダッシュボード/APIはオブザーバビリティ/制御の面のみである必要があり、オーケストレータの正確さのために必須になってはならない。

有効化 (拡張)：

- CLIの `--port` 引数が提供されたときにHTTPサーバーを起動する。
- `WORKFLOW.md` のフロントマターに `server.port` が存在する場合、HTTPサーバーを起動する。
- `server.port` は拡張設定であり、意図的にセクション5.3のコアのフロントマタースキーマの一部ではない。
- 優先順位：両方が存在する場合、CLIの `--port` は `server.port` を上書きする。
- `server.port` は整数でなければならない。正の値はそのポートをバインドする。`0` を使用して、ローカルの開発やテスト用に一時的な（エフェメラル）ポートを要求することができる。
- 明示的に別の設定がされていない限り、実装はデフォルトでループバック（`127.0.0.1` またはホストの同等物）をバインドするべきである。
- HTTPリスナー設定（例：`server.port`）の変更はホット再バインドを行う必要はない。再起動を必要とする動作で適合する。

#### 13.7.1 人間が読めるダッシュボード (`/`)

- 人間が読めるダッシュボードを `/` でホストする。
- 返されるドキュメントは、システムの現在の状態（例：アクティブなセッション、再試行の遅延、トークンの消費量、合計ランタイム、最近のイベント、健康状態/エラーインジケーター）を描写する必要がある。
- これがサーバーで生成されたHTMLであるか、以下のJSON APIを利用するクライアント側アプリであるかは、実装次第である。

#### 13.7.2 JSON REST API (`/api/v1/*`)

現在のランタイム状態と運用のデバッグ用に、`/api/v1/*` の下にJSON REST APIを提供する。

最小限のエンドポイント：

- `GET /api/v1/state`
  - 現在のシステム状態（実行中のセッション、再試行キュー/遅延、合計のトークン/ランタイム集計、最新のレート制限、およびその他の追跡された要約フィールド）の概要ビューを返す。
  - 推奨される応答の形：

    ```json
    {
      "generated_at": "2026-02-24T20:15:30Z",
      "counts": {
        "running": 2,
        "retrying": 1
      },
      "running": [
        {
          "issue_id": "abc123",
          "issue_identifier": "MT-649",
          "state": "In Progress",
          "session_id": "thread-1-turn-1",
          "turn_count": 7,
          "last_event": "turn_completed",
          "last_message": "",
          "started_at": "2026-02-24T20:10:12Z",
          "last_event_at": "2026-02-24T20:14:59Z",
          "tokens": {
            "input_tokens": 1200,
            "output_tokens": 800,
            "total_tokens": 2000
          }
        }
      ],
      "retrying": [
        {
          "issue_id": "def456",
          "issue_identifier": "MT-650",
          "attempt": 3,
          "due_at": "2026-02-24T20:16:00Z",
          "error": "no available orchestrator slots"
        }
      ],
      "codex_totals": {
        "input_tokens": 5000,
        "output_tokens": 2400,
        "total_tokens": 7400,
        "seconds_running": 1834.2
      },
      "rate_limits": null
    }
    ```

- `GET /api/v1/<issue_identifier>`
  - デバッグに役立つ、実装が追跡するすべての情報を含む、識別されたイシューのイシュー固有のランタイム/デバッグの詳細を返す。
  - 推奨される応答の形：

    ```json
    {
      "issue_identifier": "MT-649",
      "issue_id": "abc123",
      "status": "running",
      "workspace": {
        "path": "/tmp/symphony_workspaces/MT-649"
      },
      "attempts": {
        "restart_count": 1,
        "current_retry_attempt": 2
      },
      "running": {
        "session_id": "thread-1-turn-1",
        "turn_count": 7,
        "state": "In Progress",
        "started_at": "2026-02-24T20:10:12Z",
        "last_event": "notification",
        "last_message": "Working on tests",
        "last_event_at": "2026-02-24T20:14:59Z",
        "tokens": {
          "input_tokens": 1200,
          "output_tokens": 800,
          "total_tokens": 2000
        }
      },
      "retry": null,
      "logs": {
        "codex_session_logs": [
          {
            "label": "latest",
            "path": "/var/log/symphony/codex/MT-649/latest.log",
            "url": null
          }
        ]
      },
      "recent_events": [
        {
          "at": "2026-02-24T20:14:59Z",
          "event": "notification",
          "message": "Working on tests"
        }
      ],
      "last_error": null,
      "tracked": {}
    }
    ```

  - イシューが現在のメモリ上の状態にとって不明である場合、エラー応答とともに `404` を返す（例：`{\"error\":{\"code\":\"issue_not_found\",\"message\":\"...\"}}`）。

- `POST /api/v1/refresh`
  - 即時のトラッカーポーリング + 再調整サイクルをキューに入れる（ベストエフォート型のトリガー。実装は繰り返しのリクエストを合体・結合させてもよい）。
  - 推奨されるリクエストの本文：空の本文または `{}`。
  - 推奨される応答（`202 Accepted`）の形：

    ```json
    {
      "queued": true,
      "coalesced": false,
      "requested_at": "2026-02-24T20:15:30Z",
      "operations": ["poll", "reconcile"]
    }
    ```

APIの設計上の注意：

- 上記のJSONの形状は、相互運用性とデバッグの使いやすさのための推奨されるベースラインである。
- 実装はフィールドを追加してもよいが、バージョン内で既存のフィールドを壊すことは避けるべきである。
- エンドポイントは、`/refresh` などの運用トリガーを除いて、読み取り専用である必要がある。
- 定義されたルートでサポートされていないメソッドは、`405 Method Not Allowed` を返す必要がある。
- APIエラーは、`{"error":{"code":"...","message":"..."}}` などのJSONエンベロープを使用する必要がある。
- ダッシュボードがクライアント側アプリである場合、状態のロジックを複製するのではなく、このAPIを利用するべきである。
## 14. 障害モデルと回復戦略

### 14.1 障害のクラス

1. `Workflow/Config Failures` (ワークフロー/設定の障害)
   - `WORKFLOW.md` の欠落
   - 無効な YAML フロントマター
   - サポートされていないトラッカーの種類、またはトラッカーの資格情報/プロジェクトスラグの欠落
   - コーディングエージェント実行可能ファイルの欠落

2. `Workspace Failures` (ワークスペースの障害)
   - ワークスペースディレクトリの作成失敗
   - ワークスペースのデータ追加/同期の失敗（実装定義。フックから発生する可能性がある）
   - 無効なワークスペースパス設定
   - フックのタイムアウト/失敗

3. `Agent Session Failures` (エージェントセッションの障害)
   - 起動時のハンドシェイクの失敗
   - ターンの失敗/キャンセル
   - ターンのタイムアウト
   - ユーザー入力の要求（ハードエラー）
   - サブプロセスの終了
   - ストール（停滞）したセッション（非アクティブ状態）

4. `Tracker Failures` (トラッカーの障害)
   - API トランスポートエラー
   - 非200ステータス
   - GraphQL エラー
   - 不正な形式のペイロード

5. `Observability Failures` (可観測性の障害)
   - スナップショットのタイムアウト
   - ダッシュボードのレンダリングエラー
   - ログシンクの設定失敗

### 14.2 回復動作

- ディスパッチ検証の失敗：
  - 新しいディスパッチをスキップする。
  - サービスをアクティブに保つ。
  - 可能な場合は再調整(Reconciliation)を継続する。

- ワーカーの失敗：
  - 指数バックオフによる再試行に変換する。

- トラッカーの候補取得の失敗：
  - このティックをスキップする。
  - 次のティックで再試行する。

- 再調整の状態更新の失敗：
  - 現在のワーカーを維持する。
  - 次のティックで再試行する。

- ダッシュボード/ログの失敗：
  - オーケストレータをクラッシュさせない。

### 14.3 部分的な状態の回復（再起動）

現在の設計は、スケジューラの状態を意図的にインメモリにしている。

再起動後：

- 前のプロセスのメモリから再試行タイマーは復元されない。
- 実行中のセッションが回復可能であるとは見なされない。
- サービスは以下によって回復する：
  - 起動時の終了ワークスペースのクリーンアップ
  - アクティブなイシューの新たなポーリング
  - 適格な作業の再ディスパッチ

### 14.4 オペレーターの介入ポイント

オペレーターは以下の方法で動作を制御できる：

- `WORKFLOW.md`（プロンプトとほとんどのランタイム設定）の編集。
- `WORKFLOW.md` の変更は自動的に検出され、再起動なしで再適用される必要がある。
- トラッカーのイシュー状態の変更：
  - 終了状態 -> 実行中のセッションが停止され、再調整時にワークスペースがクリーンアップされる。
  - 非アクティブな状態 -> 実行中のセッションは停止されるが、クリーンアップは行われない。
- プロセスの回復またはデプロイのためのサービスの再起動（ワークフロー設定の変更を適用するための通常のパスとしてではない）。

## 15. セキュリティと運用の安全性

### 15.1 信頼境界（Trust Boundary）の前提

各実装は、独自の信頼境界を定義する。

運用上の安全性要件：

- 実装は、信頼できる環境向けか、より制限された環境向けか、あるいはその両方であるかを明確に記載すべきである。
- 実装は、自動承認されたアクション、オペレーターの承認、より厳密なサンドボックス化、またはそれらのコントロールの組み合わせのどれに依存するかを明確に述べるべきである。
- ワークスペースの分離とパスの検証は重要なベースラインのコントロールであるが、実装が選択するどのような承認およびサンドボックスポリシーの代わりになるものではない。

### 15.2 ファイルシステムの安全性要件

必須：

- ワークスペースのパスは、構成されたワークスペースルートの下にとどまらなければならない。
- コーディングエージェントの cwd（カレントディレクトリ）は、現在の実行のイシューごとのワークスペースパスでなければならない。
- ワークスペースのディレクトリ名には、サニタイズされた識別子を使用しなければならない。

ポート(port)に対する推奨される追加の強化：

- 専用のOSユーザーで実行する。
- ワークスペースルートのアクセス許可を制限する。
- 可能であれば、専用ボリュームにワークスペースルートをマウントする。

### 15.3 シークレットの取り扱い

- ワークフロー設定での `$VAR` 間接参照をサポートする。
- APIトークンやシークレットの環境変数の値をログに記録しない。
- シークレットを出力することなく存在を検証する。

### 15.4 フックスクリプトの安全性

ワークスペースフックは、`WORKFLOW.md` からの任意のシェルスクリプトである。

影響：

- フックは完全に信頼できる設定である。
- フックはワークスペースディレクトリ内で実行される。
- フックの出力はログ内で切り詰められるべきである。
- オーケストレータがハングアップするのを防ぐため、フックのタイムアウトは必須である。

### 15.5 ハーネスの強化（Hardening）に関するガイダンス

機密データや外部から制御されるコンテンツを含む可能性のあるリポジトリ、イシュートラッカー、およびその他の入力に対してCodexエージェントを実行することは危険を伴う場合がある。寛容なデプロイメントは、エージェントが有害なコマンドを実行したり、過度に強力な統合を使用したりするよう誘導された場合、データ漏洩、破壊的な変更、またはマシンの完全な侵害につながる可能性がある。

実装は、独自のリスクプロファイルを明示的に評価し、適切な場所で実行ハーネスを強化する必要がある。この仕様は意図的に単一の強化姿勢を義務付けていないが、トラッカーのデータ、リポジトリのコンテンツ、プロンプトの入力、またはツールの引数が通常のワークフローの内部に由来しているからといって、完全に信頼できると見なすべきではない。

考えられる強化対策は以下の通りである：

- 最大限に寛容な設定で実行するのではなく、この仕様の別の場所で説明されているCodexの承認およびサンドボックス設定を強化する。
- 組み込みのCodexポリシー制御を超えて、OS/コンテナ/VMのサンドボックス化、ネットワーク制限、または個別の資格情報など、外部の隔離レイヤーを追加する。
- ディスパッチの対象となるLinearイシュー、プロジェクト、チーム、ラベル、またはその他のトラッカーソースをフィルタリングし、信頼できない、または範囲外のタスクが自動的にエージェントに到達しないようにする。
- オプションの `linear_graphql` ツールを絞り込み、ワークスペース全体での一般的なトラッカーアクセスを公開するのではなく、意図したプロジェクトスコープ内のデータのみを読み取りまたはミューテーションできるようにする。
- エージェントが利用できるクライアントサイドツール、資格情報、ファイルシステムパス、ネットワークの宛先のセットを、ワークフローに必要な最小限に減らす。

正しい制御はデプロイメント固有であるが、実装はそれらを明確に文書化し、ハーネスの強化をオプションの付け足しではなくコアのセキュリティモデルの一部として扱うべきである。

## 16. リファレンスアルゴリズム (言語非依存)

### 16.1 サービスの起動

```text
function start_service():
  configure_logging()
  start_observability_outputs()
  start_workflow_watch(on_change=reload_and_reapply_workflow)

  state = {
    poll_interval_ms: get_config_poll_interval_ms(),
    max_concurrent_agents: get_config_max_concurrent_agents(),
    running: {},
    claimed: set(),
    retry_attempts: {},
    completed: set(),
    codex_totals: {input_tokens: 0, output_tokens: 0, total_tokens: 0, seconds_running: 0},
    codex_rate_limits: null
  }

  validation = validate_dispatch_config()
  if validation is not ok:
    log_validation_error(validation)
    fail_startup(validation)

  startup_terminal_workspace_cleanup()
  schedule_tick(delay_ms=0)

  event_loop(state)
```

### 16.2 ポーリングとディスパッチのティック

```text
on_tick(state):
  state = reconcile_running_issues(state)

  validation = validate_dispatch_config()
  if validation is not ok:
    log_validation_error(validation)
    notify_observers()
    schedule_tick(state.poll_interval_ms)
    return state

  issues = tracker.fetch_candidate_issues()
  if issues failed:
    log_tracker_error()
    notify_observers()
    schedule_tick(state.poll_interval_ms)
    return state

  for issue in sort_for_dispatch(issues):
    if no_available_slots(state):
      break

    if should_dispatch(issue, state):
      state = dispatch_issue(issue, state, attempt=null)

  notify_observers()
  schedule_tick(state.poll_interval_ms)
  return state
```

### 16.3 アクティブな実行の再調整

```text
function reconcile_running_issues(state):
  state = reconcile_stalled_runs(state)

  running_ids = keys(state.running)
  if running_ids is empty:
    return state

  refreshed = tracker.fetch_issue_states_by_ids(running_ids)
  if refreshed failed:
    log_debug("keep workers running")
    return state

  for issue in refreshed:
    if issue.state in terminal_states:
      state = terminate_running_issue(state, issue.id, cleanup_workspace=true)
    else if issue.state in active_states:
      state.running[issue.id].issue = issue
    else:
      state = terminate_running_issue(state, issue.id, cleanup_workspace=false)

  return state
```

### 16.4 1つのイシューのディスパッチ

```text
function dispatch_issue(issue, state, attempt):
  worker = spawn_worker(
    fn -> run_agent_attempt(issue, attempt, parent_orchestrator_pid) end
  )

  if worker spawn failed:
    return schedule_retry(state, issue.id, next_attempt(attempt), {
      identifier: issue.identifier,
      error: "failed to spawn agent"
    })

  state.running[issue.id] = {
    worker_handle,
    monitor_handle,
    identifier: issue.identifier,
    issue,
    session_id: null,
    codex_app_server_pid: null,
    last_codex_message: null,
    last_codex_event: null,
    last_codex_timestamp: null,
    codex_input_tokens: 0,
    codex_output_tokens: 0,
    codex_total_tokens: 0,
    last_reported_input_tokens: 0,
    last_reported_output_tokens: 0,
    last_reported_total_tokens: 0,
    retry_attempt: normalize_attempt(attempt),
    started_at: now_utc()
  }

  state.claimed.add(issue.id)
  state.retry_attempts.remove(issue.id)
  return state
```

### 16.5 ワーカーの試行 (ワークスペース + プロンプト + エージェント)

```text
function run_agent_attempt(issue, attempt, orchestrator_channel):
  workspace = workspace_manager.create_for_issue(issue.identifier)
  if workspace failed:
    fail_worker("workspace error")

  if run_hook("before_run", workspace.path) failed:
    fail_worker("before_run hook error")

  session = app_server.start_session(workspace=workspace.path)
  if session failed:
    run_hook_best_effort("after_run", workspace.path)
    fail_worker("agent session startup error")

  max_turns = config.agent.max_turns
  turn_number = 1

  while true:
    prompt = build_turn_prompt(workflow_template, issue, attempt, turn_number, max_turns)
    if prompt failed:
      app_server.stop_session(session)
      run_hook_best_effort("after_run", workspace.path)
      fail_worker("prompt error")

    turn_result = app_server.run_turn(
      session=session,
      prompt=prompt,
      issue=issue,
      on_message=(msg) -> send(orchestrator_channel, {codex_update, issue.id, msg})
    )

    if turn_result failed:
      app_server.stop_session(session)
      run_hook_best_effort("after_run", workspace.path)
      fail_worker("agent turn error")

    refreshed_issue = tracker.fetch_issue_states_by_ids([issue.id])
    if refreshed_issue failed:
      app_server.stop_session(session)
      run_hook_best_effort("after_run", workspace.path)
      fail_worker("issue state refresh error")

    issue = refreshed_issue[0] or issue

    if issue.state is not active:
      break

    if turn_number >= max_turns:
      break

    turn_number = turn_number + 1

  app_server.stop_session(session)
  run_hook_best_effort("after_run", workspace.path)

  exit_normal()
```

### 16.6 ワーカー終了と再試行処理

```text
on_worker_exit(issue_id, reason, state):
  running_entry = state.running.remove(issue_id)
  state = add_runtime_seconds_to_totals(state, running_entry)

  if reason == normal:
    state.completed.add(issue_id)  # bookkeeping only (記録用のみ)
    state = schedule_retry(state, issue_id, 1, {
      identifier: running_entry.identifier,
      delay_type: continuation
    })
  else:
    state = schedule_retry(state, issue_id, next_attempt_from(running_entry), {
      identifier: running_entry.identifier,
      error: format("worker exited: %reason")
    })

  notify_observers()
  return state
```

```text
on_retry_timer(issue_id, state):
  retry_entry = state.retry_attempts.pop(issue_id)
  if missing:
    return state

  candidates = tracker.fetch_candidate_issues()
  if fetch failed:
    return schedule_retry(state, issue_id, retry_entry.attempt + 1, {
      identifier: retry_entry.identifier,
      error: "retry poll failed"
    })

  issue = find_by_id(candidates, issue_id)
  if issue is null:
    state.claimed.remove(issue_id)
    return state

  if available_slots(state) == 0:
    return schedule_retry(state, issue_id, retry_entry.attempt + 1, {
      identifier: issue.identifier,
      error: "no available orchestrator slots"
    })

  return dispatch_issue(issue, state, attempt=retry_entry.attempt)
```
## 17. テストおよび検証マトリックス

適合する実装は、この仕様で定義された動作をカバーするテストを含めるべきである。

検証プロファイル：

- `Core Conformance` (コアへの適合性): すべての適合する実装に必要な決定論的テスト。
- `Extension Conformance` (拡張の適合性): 実装が出荷することを選択したオプション機能にのみ必要。
- `Real Integration Profile` (実際の統合プロファイル): 本番環境での使用前に推奨される、環境に依存するスモーク/統合チェック。

特に明記されていない限り、セクション17.1から17.7は `Core Conformance`（コアへの適合性）である。「... が実装されている場合」で始まる箇条書きは `Extension Conformance`（拡張の適合性）である。

### 17.1 ワークフローと設定の解析

- ワークフローファイルパスの優先順位：
  - 指定されている場合は、明示的なランタイムパスが使用される
  - 明示的なランタイムパスが指定されていない場合は、cwdのデフォルトの `WORKFLOW.md` となる
- ワークフローファイルの変更が検出され、再起動なしで再読み込み/再適用がトリガーされる
- 無効なワークフローのリロード時は、最後に確認された有効な設定を保持し、オペレーターが視認可能なエラーを出力する
- `WORKFLOW.md` が欠落している場合、型付けされたエラーを返す
- 無効なYAMLフロントマターの場合、型付けされたエラーを返す
- フロントマターがマップでない場合、型付けされたエラーを返す
- オプション値が欠落している場合、設定のデフォルト値が適用される
- `tracker.kind` の検証により、現在サポートされている種類（`linear`）が強制される
- `tracker.api_key` が機能する（`$VAR` の間接参照を含む）
- トラッカーのAPIキーとパス値に対する `$VAR` の解決が機能する
- `~` パス展開が機能する
- `codex.command` はシェルコマンドの文字列として保持される
- 状態ごとの同時実行の上書きマップは、状態名を正規化し、無効な値を無視する
- プロンプトテンプレートは `issue` と `attempt` をレンダリングする
- 不明な変数がある場合、プロンプトのレンダリングは失敗する（厳密モード）

### 17.2 ワークスペースマネージャと安全性

- イシュー識別子ごとの決定論的ワークスペースパス
- 存在しないワークスペースディレクトリが作成される
- 既存のワークスペースディレクトリが再利用される
- ワークスペースの場所にディレクトリ以外の既存のパスがある場合、安全に処理される（実装のポリシーに従って置換または失敗）
- オプションのワークスペースデータの追加/同期のエラーが表面化する
- 準備中に一時的なアーティファクト（`tmp`、`.elixir_ls`）が削除される
- `after_create` フックは新しいワークスペース作成時にのみ実行される
- `before_run` フックは各試行の前に実行され、失敗/タイムアウトは現在の試行を中止する
- `after_run` フックは各試行の後に実行され、失敗/タイムアウトはログに記録されて無視される
- `before_remove` フックはクリーンアップ時に実行され、失敗/タイムアウトは無視される
- ワークスペースパスのサニタイズとルート包含の不変条件は、エージェントの起動前に強制される
- エージェントの起動では、イシューごとのワークスペースパスをcwdとして使用し、ルート外のパスを拒否する

### 17.3 イシュートラッカークライアント

- 候補イシューの取得では、アクティブな状態とプロジェクトスラグが使用される
- Linearクエリは、指定されたプロジェクトフィルターフィールド（`slugId`）を使用する
- 空の `fetch_issues_by_states([])` はAPIを呼び出さずに空を返す
- ページネーションは複数のページ間で順序を保持する
- ブロッカーは、タイプが `blocks` の逆リレーションから正規化される
- ラベルは小文字に正規化される
- IDによるイシュー状態の更新は、最小限の正規化されたイシューを返す
- イシュー状態の更新クエリは、セクション11.2で指定されているGraphQL IDタイプ（`[ID!]`）を使用する
- リクエストエラー、非200、GraphQLエラー、不正なペイロードに対するエラーマッピング

### 17.4 オーケストレータのディスパッチ、再調整、再試行

- ディスパッチのソート順は、優先度、作成時間の古い順である
- 終了していないブロッカーを持つ `Todo` イシューは適格ではない
- 終了したブロッカーを持つ `Todo` イシューは適格である
- アクティブな状態のイシュー更新は、実行中エントリの状態を更新する
- 非アクティブな状態は、ワークスペースをクリーンアップせずに実行中のエージェントを停止する
- 終了状態は、実行中のエージェントを停止し、ワークスペースをクリーンアップする
- 実行中のイシューがない再調整(Reconciliation)は何も行わない (no-op)
- 正常なワーカー終了は、短い継続再試行（attempt 1）をスケジュールする
- 異常なワーカー終了は、10秒ベースの指数バックオフで再試行をインクリメントする
- 再試行バックオフの上限は、設定された `agent.max_retry_backoff_ms` を使用する
- 再試行キューのエントリには、試行回数(attempt)、期限、識別子、およびエラーが含まれる
- ストール検出により、ストールしたセッションが強制終了され、再試行がスケジュールされる
- スロットが枯渇した場合、明示的なエラー理由で再試行が再キューイングされる
- スナップショットAPIが実装されている場合、実行中の行、再試行の行、トークン合計、およびレート制限を返す
- スナップショットAPIが実装されている場合、タイムアウト/利用不可のケースが表面化する

### 17.5 コーディングエージェントのアプリケーションサーバークライアント

- 起動コマンドはワークスペースのcwdを使用し、`bash -lc <codex.command>` を呼び出す
- 起動ハンドシェイクは `initialize`、`initialized`、`thread/start`、`turn/start` を送信する
- `initialize` には、ターゲットとなるCodexアプリサーバープロトコルで必要とされるクライアントID/機能ペイロードが含まれる
- ポリシー関連の起動ペイロードは、実装の文書化された承認/サンドボックス設定を使用する
- `thread/start` および `turn/start` は、ネストされたIDを解析し、`session_started` を発行する
- リクエスト/レスポンスの読み取りタイムアウトが強制される
- ターンのタイムアウトが強制される
- 部分的なJSON行は改行までバッファリングされる
- Stdoutとstderrは個別に処理され、プロトコルのJSONはstdoutからのみ解析される
- JSON以外のstderrの行はログに記録されるが、解析のクラッシュは引き起こさない
- コマンド/ファイル変更の承認は、実装の文書化されたポリシーに従って処理される
- サポートされていない動的ツールの呼び出しは、セッションを停止（ストール）することなく拒否される
- ユーザー入力の要求は実装の文書化されたポリシーに従って処理され、無期限に停止することはない
- 使用状況(Usage)およびレート制限ペイロードは、ネストされたペイロード形状から抽出される
- 承認、ユーザー入力要求のシグナル、および使用状況/レート制限のテレメトリについて、互換性のあるペイロードのバリエーションは、同じ論理的意味を保持している場合は許容される
- オプションのクライアントサイドツールが実装されている場合、起動時のハンドシェイクで、ターゲットのアプリサーバーのバージョンが発見するために必要な、サポートされているツール仕様をアドバタイズ(通知)する
- オプションの `linear_graphql` クライアントサイドツールの拡張機能が実装されている場合：
  - ツールがセッションに通知される
  - 有効な `query` / `variables` の入力は、構成されたLinear認証に対して実行される
  - 最上位のGraphQL `errors` は、GraphQLの本文を保持したまま `success=false` を生成する
  - 無効な引数、認証の欠落、およびトランスポートの失敗は、構造化された失敗ペイロードを返す
  - サポートされていないツール名は、セッションを停止（ストール）することなく依然として失敗する

### 17.6 オブザーバビリティ

- 検証の失敗はオペレーターが視認できる
- 構造化ログには、イシュー/セッションのコンテキストフィールドが含まれる
- ロギングシンクの障害は、オーケストレーションをクラッシュさせない
- トークン/レート制限の集計は、繰り返されるエージェントの更新全体で正確さを維持する
- 人間が読めるステータスサーフェスが実装されている場合、それはオーケストレータの状態から駆動され、正確性には影響しない
- 人間化されたイベントの要約が実装されている場合、オーケストレータの動作を変更することなく、主要なラッパー/エージェントイベントクラスをカバーする

### 17.7 CLIとホストのライフサイクル

- CLIはオプションの位置引数としてのワークフローパス（`path-to-WORKFLOW.md`）を受け入れる
- ワークフローパスの引数が指定されていない場合、CLIは `./WORKFLOW.md` を使用する
- 存在しない明示的なワークフローパス、またはデフォルトの `./WORKFLOW.md` が欠落している場合、CLIはエラーになる
- CLIは起動の失敗をクリーンに表面化する
- アプリケーションが起動し、正常にシャットダウンした場合、CLIは成功として終了する
- 起動が失敗したか、ホストプロセスが異常終了した場合、CLIはゼロ以外の値で終了する

### 17.8 実際の統合プロファイル (推奨)

これらのチェックは本番環境の準備状況の確認として推奨され、資格情報、ネットワークアクセス、または外部サービスの権限が利用できない場合は、CIでスキップしてもよい。

- 実際のトラッカースモークテストは、`LINEAR_API_KEY` または文書化されたローカルのブートストラップメカニズム（例：`~/.linear_api_key`）によって提供される有効な資格情報を使用して実行できる。
- 実際の統合テストでは、隔離されたテスト識別子/ワークスペースを使用し、可能であればトラッカーのアーティファクトをクリーンアップするべきである。
- スキップされた実際の統合テストは、黙ってパスしたとみなすのではなく、スキップされたとして報告するべきである。
- 実際の統合プロファイルがCIまたはリリースの検証で明示的に有効になっている場合、失敗した場合はそのジョブを失敗させるべきである。

## 18. 実装チェックリスト (Definition of Done: 完了の定義)

セクション17と同じ検証プロファイルを使用する：

- セクション18.1 = `Core Conformance` (コアへの適合性)
- セクション18.2 = `Extension Conformance` (拡張の適合性)
- セクション18.3 = `Real Integration Profile` (実際の統合プロファイル)

### 18.1 適合のために必須

- ワークフローのパス選択は、明示的なランタイムパスとcwdのデフォルトをサポートする
- YAMLフロントマター + プロンプト本文の分割による `WORKFLOW.md` ローダー
- デフォルト値と `$` の解決を伴う型付きの設定レイヤー
- 設定とプロンプトのための動的な `WORKFLOW.md` の監視/再読み込み/再適用
- 単一権限のミュータブルな状態を持つポーリングオーケストレータ
- 候補の取得 + 状態の更新 + 終了イシューの取得を備えたイシュートラッカークライアント
- サニタイズされたイシューごとのワークスペースを持つワークスペースマネージャ
- ワークスペースのライフサイクルフック（`after_create`、`before_run`、`after_run`、`before_remove`）
- フックタイムアウト設定（`hooks.timeout_ms`、デフォルト `60000`）
- JSON行プロトコルを持つコーディングエージェントのアプリサーバーサブプロセス用のクライアント
- Codex起動コマンド設定（`codex.command`、デフォルト `codex app-server`）
- `issue` および `attempt` 変数による厳密なプロンプトレンダリング
- 正常終了後の継続再試行を備えた、指数再試行キュー
- 設定可能な再試行バックオフの上限（`agent.max_retry_backoff_ms`、デフォルト5分）
- 終了/非アクティブなトラッカー状態で実行を停止する再調整(Reconciliation)機能
- 終了イシューのためのワークスペースクリーンアップ（起動時の一括処理 + アクティブな移行）
- `issue_id`、`issue_identifier`、および `session_id` を含む構造化ログ
- オペレーターが視認可能な可観測性（構造化ログ、オプションのスナップショット/ステータスサーフェス）

### 18.2 推奨される拡張 (適合のためには必須ではない)

- オプションのHTTPサーバーは、`server.port` よりもCLIの `--port` を優先し、安全なデフォルトのバインドホストを使用し、出荷された場合はセクション13.7のベースラインエンドポイント/エラーセマンティクスを公開する。
- オプションの `linear_graphql` クライアントサイドツールの拡張は、設定されたSymphony認証を使用して、アプリサーバーセッションを通じて生のLinear GraphQLアクセスを公開する。
- TODO：プロセス再起動間で再試行キューとセッションメタデータを永続化する。
- TODO：UI実装の詳細を規定することなく、ワークフローのフロントマターで可観測性設定を構成可能にする。
- TODO：エージェントのツール経由のみではなく、オーケストレータ内にファーストクラスのトラッカー書き込みAPI（コメント/状態遷移）を追加する。
- TODO：Linear以外のプラグイン可能なイシュートラッカーアダプターを追加する。

### 18.3 本番稼働前の運用検証 (推奨)

- 有効な資格情報とネットワークアクセスを使用して、セクション17.8の `Real Integration Profile` を実行する。
- ターゲットホストのOS/シェル環境で、フックの実行とワークフローのパス解決を検証する。
- オプションのHTTPサーバーが出荷されている場合は、ターゲット環境で設定されたポートの動作と、ループバック/デフォルトのバインドの想定を検証する。

## 付録 A. SSH ワーカー拡張 (オプション)

この付録では、Symphonyが1つの中心的なオーケストレータを維持しながら、SSH経由で1つ以上のリモートホスト上でワーカーを実行する、一般的な拡張プロファイルについて説明する。

### A.1 実行モデル

- オーケストレータは引き続き、ポーリング、要求(claim)、再試行、および再調整の単一の信頼できる情報源（Single Source of Truth）である。
- `worker.ssh_hosts` は、リモート実行の候補となるSSHの宛先を提供する。
- 各ワーカー実行は一度に1つのホストに割り当てられ、そのホストはイシューのワークスペースとともに、実行の有効な実行IDの一部となる。
- `workspace.root` は、オーケストレータのホストではなく、リモートホスト上で解釈される。
- コーディングエージェントのアプリサーバーは、ローカルのサブプロセスとしてではなくSSHのstdio経由で起動されるため、コマンドがリモートで実行される場合でも、セッションのライフサイクルは引き続きオーケストレータが所有する。
- 1つのワーカーライフタイム内の継続ターンは、同じホストとワークスペースに留まる必要がある。
- リモートホストは、ローカルワーカー環境と同じ基本契約を満たす必要がある：到達可能なシェル、書き込み可能なワークスペースルート、コーディングエージェントの実行可能ファイル、および必要な認証またはリポジトリの前提条件。

### A.2 スケジューリングに関する注意

- SSHホストは、ディスパッチのプールとして扱われる場合がある。
- 実装は、再試行時に以前使用したホストが引き続き利用可能であれば、それを優先してもよい。
- `worker.max_concurrent_agents_per_host` は、設定されたSSHホスト全体にわたるオプションの共有されるホストごとの上限である。
- すべてのSSHホストが容量に達している場合、別の実行モードに黙ってフォールバックするのではなく、ディスパッチは待機するべきである。
- 実装は、作業が意味のある開始をする前に元のホストが利用できなくなった場合、別のホストにフェイルオーバーしてもよい。
- 実行によってすでに副作用が発生している場合、別のホストでの透過的な再実行は、目に見えないフェイルオーバーとしてではなく、新しい試行として扱われるべきである。

### A.3 考慮すべき問題点

- リモート環境のドリフト：
  - 各ホストには、期待されるシェル環境、コーディングエージェントの実行可能ファイル、認証、およびリポジトリの前提条件が必要である。
- ワークスペースの局所性（Locality）：
  - ワークスペースは通常ホストのローカルであるため、イシューを別のホストに移動することは、共有ストレージが存在しない限り、通常はコールドリスタートになる。
- パスとコマンドの安全性：
  - 実行がマシンの境界を越えると、リモートのパス解決、シェルの引用符(クォーティング)、およびワークスペース境界のチェックがより重要になる。
- 起動とフェイルオーバーのセマンティクス：
  - 同じチケットが誤って複数のホストで再実行されないように、実装はホストの接続/起動の失敗とワークスペース内のエージェントの失敗を区別するべきである。
- ホストの健康状態と飽和状態：
  - 停止または過負荷になったホストは利用可能な容量を減らすべきであり、重複実行やローカル作業への偶発的なフォールバックを引き起こすべきではない。
- クリーンアップと可観測性：
  - オペレーターは、どのホストが実行を所有しているか、そのワークスペースがどこにあるか、適切なマシンでクリーンアップが行われたかどうかを知る必要がある。
